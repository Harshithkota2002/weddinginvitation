Drop gallery photos here named 1.jpg, 2.jpg, 3.jpg ... 9.jpg

They map to the items in lib/config.ts (gallery array). Any missing
file falls back to a themed floral placeholder, so the gallery always
looks complete. Add/remove items by editing the `gallery` array in
lib/config.ts.
